sudo: required
language: ruby
cache: bundler
rvm:
- 3.0.1
jobs:
  include:
  - stage: test
    name: rubocop
    script: bundle exec rubocop
  - stage: test
    name: rspec
    script: gem install bundler & bundle exec rspec